import { createContext } from "react"

/***** 
Create a React context. Used to share the state throughout the app
Initialisation: "null"
*****/

const Context = createContext(null)
export default Context